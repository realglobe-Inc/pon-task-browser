/**
 * Test case for url.
 * Runs with mocha.
 */
'use strict'

const url = require('../lib/core/url.js')
const assert = require('assert')

describe('url', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Url', async () => {

  })
})

/* global describe, before, after, it */
