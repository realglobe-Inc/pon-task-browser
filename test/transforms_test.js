/**
 * Test case for transforms.
 * Runs with mocha.
 */
'use strict'

const transforms = require('../lib/transforms.js')
const assert = require('assert')
const co = require('co')

describe('transforms', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Transforms', () => co(function * () {

  }))
})

/* global describe, before, after, it */
