/**
 * Test case for assertEntryExists.
 * Runs with mocha.
 */
'use strict'

const assertEntryExists = require('../lib/helpers/assertEntryExists.js')
const assert = require('assert')

describe('assert-entry-exists', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Assert entry exists', async () => {

  })
})

/* global describe, before, after, it */
