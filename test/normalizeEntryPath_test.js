/**
 * Test case for normalizeEntryPath.
 * Runs with mocha.
 */
'use strict'

const normalizeEntryPath = require('../lib/helpers/normalizeEntryPath.js')
const assert = require('assert')

describe('normalize-entry-path', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Normalize entry path', async () => {

  })
})

/* global describe, before, after, it */
